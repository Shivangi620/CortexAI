[tool.ruff]
line-length = 120
target-version = "py310"
extend-exclude = [
  "node_modules",
  "venv",
  "frontend/static",
  "backend/scratch",
]

[tool.ruff.lint]
select = ["E9", "F"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]
